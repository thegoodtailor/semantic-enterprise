== Exploring the Glued Ontology

////
CHAPTER ABSTRACT (tracking; not rendered in the book)

Flat Gold answers the anticipated question and a single domain's Silver-as-Gold view
answers the drill within one vocabulary; the question that crosses Trading, Compliance,
and Custody in one line of thought is served by neither, and it is the permanent
condition of analysis. The glued ontology — the colimit of domain theories and their
bridges — is already an enterprise-spanning dimensional model, and it is consumed by
navigating it live, Silver-direct, through the semantic layer: each pivot composes the
target domain's surface across a bridge, at the right time grain via the as-of rule,
minting a published answer whose trust grade is the weakest of the surfaces crossed.
Every crossing is a change of register, and polysemy is its central hazard — a word like
"volume" carries a different measure in Finance and Trading, and the meaning shift is
detectable before the target register loads only through a thin, always-resident
polysemy index. The hard problem is the joined cell: composed access is the intersection
of the contributing domains' policies, declared at the bridge and never inferred at
query time, with withheld cells typed "restricted" and named to the grant that completes
them, existence-sensitive columns declared out of scope, and mixed-authorization
aggregates failing closed over the intersection. Live composition is the most expensive
query shape in the estate, so latency budgets, rollup routing, and answer-ledger-driven
materialization of the high-traffic crossings keep the live explore for the long tail
nobody anticipated. Business users and data scientists are walking the same surface, and
the difference between an organization that can and one that cannot is whether the
bridges exist.

Argument:
1. Flat Gold answers only the anticipated question, and Silver-as-Gold views answer only
   single-domain drill-and-slice; the permanent condition of analysis is that the next
   question crosses domains nobody pre-joined.
2. The colimit constructed in Chapter 4 can be turned into a consumption surface: each
   domain contributes its Silver dimensional model as a governed view, and bridges
   compose those surfaces at pivot time via spans through shared sub-theories, with as-
   of joins at the right time grain.
3. Every cross-domain pivot is a change of register — a cognitive configuration, not a
   filter — so crossing composes two sets of definitions, heuristics, and caveats at
   once.
4. Polysemy is the central hazard of the crossing (Finance 'volume' vs Trading 'volume'
   completes without error and returns a precisely wrong number), and a bootstrap circle
   — you detect a meaning shift only if you already hold both meanings — is resolved by
   a thin, always-resident polysemy index that makes the shift detectable before the
   target register loads.
5. The bridge is the governed mapping that decides what the composed view means: which
   concepts translate, which conflict rules hold where domains disagree, which temporal
   alignment makes the join legal; the crossing runs a pre-flight and the resolution
   persists as session state.
6. The hard problem is security of the joined cell: composed access must be the
   intersection of the contributing domains' policies, declared at the bridge, never
   inferred at query time — silent widening is privilege escalation, silent dropping is
   a partial answer that looks complete.
7. Withholding must be visible (a typed 'restricted' naming the completing grant),
   except where existence itself is sensitive, in which case the column is declared out
   of scope; aggregates over mixed-authorization cells fail closed and compute over the
   intersection only, carrying that scope on the answer.
8. Live cross-domain composition is the most expensive query shape in the estate, so the
   layer owns per-hop latency budgets, routes to rollups, and the answer ledger
   identifies the high-traffic cross-domain questions that earn materialization as
   governed flat Gold, leaving live exploration for the unanticipated long tail.
9. Because each pivot mints a published answer with identity, temporal coordinates, and
   composed (weakest-wins) trust grade, and the session queries only the sub-colimit of
   domains reached, the surface stays citable and answerable as the estate grows.
10. The same surface serves the business user's self-serve pivoting and the data
   scientist's cross-domain feature discovery; what separates capable organizations is
   whether the bridges exist.
Conclusions:
- The enterprise-spanning dimensional model is navigated live through the semantic layer
  as the sub-colimit of the session's domains, never materialized as a single physical
  star.
- Composed access across a bridge is the intersection of contributing domains' policies,
  declared at the bridge, with withholding visible ('restricted' plus the completing
  grant) or the column declared out of scope where existence is sensitive.
- Composed aggregates fail closed: they compute over the consumer's intersection only
  and carry that scope on the answer.
- Polysemy must be detectable before the target register loads, via a thin always-
  resident polysemy index, so the pivot asks 'which volume?' instead of guessing.
- Materialize only the highest-traffic cross-domain questions as governed flat Gold —
  the answer ledger says which earn it, the colimit governs their joins — and keep live
  exploration for the long tail.
- A missing or stale bridge must surface as a named gap mid-pivot, not a wrong number;
  the gap becomes the next morphism to write.
Creates:
- Live colimit navigation — the glued ontology consumed as a session-scoped, Silver-
  direct, cross-domain dimensional surface (the sub-colimit of domains reached)
- The intersection rule for composed access: "composed access is the intersection,
  declared at the bridge, never inferred at query time"
- The typed 'restricted' cell — visible withholding that names the grant that would
  complete the view
- The out-of-scope declaration for existence-sensitive columns (the bridge hides the gap
  rather than advertising it)
- Fail-closed composed aggregates that compute over the consumer's intersection and
  carry that scope on the answer
- The always-resident polysemy index — one line per domain sense of every multi-sense
  glossary term, resolving the register-loading bootstrap circle
- Register-switching as session mechanics: registers as cognitive configurations,
  composed on crossing, with sense resolutions persisted as session state
- The bridge pre-flight for domain crossings: explicit target-domain access,
  thin/missing-layer surfacing, polysemy mapping before formulas, declared join path
- Bridges as the locus where composed row/column policy, conflict rules, temporal
  alignment, and clean-room requirements are declared and recorded
- Per-hop latency budgets plus answer-ledger-driven materialization of high-traffic
  cross-domain questions as governed flat Gold
- Figure: Cross-Ontology Dimensional Exploration (fig-cross-ontology-dimensional) —
  domain dimensional models composed Silver-direct through bridges into one navigable
  surface
Demolishes:
- Materializing the enterprise model as a giant physical star or pre-built cross-domain
  marts for every question
- The rogue join — analysts hand-joining across domains outside machinery that knows
  meaning and access
- Silent widening of access through composition (the privilege-escalation engine)
- Silent dropping of restricted columns (the partial answer that looks complete)
- Aggregates that silently include or exclude cells the consumer cannot see
- Treating polysemous terms as shared concepts — letting a calculation complete without
  error and return a precisely wrong number
- Naive temporal joins across domains (a fiscal-calendar table joined to a UTC one)
- Governed exploration that ignores the latency bar — a five-minute governed explore
  losing to a ten-second bypass
////

The flat Gold table answers the question it was built for, brilliantly and cheaply, and not one question more. The analyst who asks "daily P&L by desk by strategy" gets a perfect answer; the analyst who then asks "...and how does that break down by the counterparty's custody region, for counterparties onboarded in the last year?" has left the table behind. The second question crosses three domains — Trading, Compliance, Custody — and no single flat table was built for it, because nobody anticipated it. That is the permanent condition of analysis: the next question is not on the menu.

The book has so far given two consumption surfaces. Flat Gold, for the anticipated question. And Silver-as-Gold views, which expose a *single domain's* dimensional model through the semantic layer — grain-defended, SCD2, conformed — so an analyst can drill and slice within Trading without a physical star schema to trip over. Neither is the thing a business user or a data scientist wants when they are exploring, which is to pivot freely across the *whole* business — to start in revenue, swing into trading volume, land on counterparty risk, and finish in custody positions, in one unbroken line of thought, without anyone having pre-built the join.

That capability is the consumption face of the colimit. The glued ontology — the domains and the bridges between them — *is* a dimensional model that spans the enterprise. You do not materialize it as a giant star; you navigate it, live, Silver-direct, through the semantic layer. Live composition of the colimit means no pre-built mart and no rogue join — every hop through machinery that already knows what the words mean and who may see them. This chapter is about what that navigation is, how it works mechanically, and the one hard problem it raises.

=== One Exploratory Session, Many Domains

Picture the session, not the query. A risk analyst starts in Risk: counterparty exposure, drawn from the Risk domain's Silver dimensional model exposed as a governed view. She drills by counterparty. She wants instrument detail — that lives in Trading — so she pivots, and the semantic layer composes the Trading dimensions onto her current view through the Trading–Risk bridge, resolving that "exposure" means something different on each side — PFE-inclusive in Risk, mark-to-market in Trading — and declaring which it is using. She pivots again to custody region; the Custody bridge carries her there. One last pivot — is this counterparty's KYC review current? — crosses into Compliance, where her entitlements will shortly matter. Four domains, one session, no pre-built mart, every hop governed.

Mechanically, this is the colimit doing exactly what Chapter 4 said it does, turned into a consumption surface:

* *Each domain contributes its dimensional model Silver-direct.* The drill-and-slice power comes from Silver — grain, conformed concepts, history-where-tiered — exposed as a governed view, never a physical star in Gold. The analyst sees a logical dimensional surface; the semantic layer generates the SQL.
* *Bridges compose the surfaces.* A pivot across a domain boundary loads the bridge between the two domains (the same span-through-a-shared-sub-theory from "Multi-Domain Composition") and joins on the shared concept — at the right time grain, via the As-Of Join rule the Time chapter is about to establish, because cross-domain joins are exactly where time-between-entities bites.
* *Register-switching resolves polysemy as she crosses.* "Position," "exposure," "volume" mean different things per domain; the semantic layer holds each domain's definition and declares which one the composed view is using, so the pivot does not silently change what a word means.
* *Every hop is a published answer.* Each pivot's result is minted into the answer ledger — the artifact "The Death of the Dashboard" will build — with identity, temporal coordinates, and the composed trust grade of the surfaces it crossed, the weakest grade winning under the propagation rule "By Any Means Necessary" will define. The session that ends in a board pack is citable hop by hop.
* *The view is the colimit, scoped to the session.* She is never querying "the whole enterprise model." She is querying the sub-colimit of the domains she has reached, which is why this stays answerable as the estate grows.

[#fig-cross-ontology-dimensional]
.Exploring the Glued Ontology — domain dimensional models composed Silver-direct through bridges into one navigable surface
image::figures/cross-ontology-dimensional.png[Cross-Ontology Dimensional Exploration,width=720,align=center]

=== Registers, Polysemy, and the Bridge That Decides

The session works because every pivot is a change of register. While the analyst is in Risk, the session is configured by Risk's Gold surfaces, definitions, and heuristics — a scoped vocabulary that functions as the system prompt of whatever serves her, exploration tool today or the agents of the chapters ahead. The register is a cognitive configuration rather than a filter: the same machinery in the Finance register and in the Trading register is two different reasoners, and a pivot across a boundary composes the configurations — two sets of definitions, heuristics, and caveats active at once.

Polysemy is the central practical hazard of the crossing. Take "volume." In Finance it typically means revenue volume — transaction value over a period, base-currency denominated, subject to accrual rules. In Trading it means executed trade volume — count or notional, real-time, segmented by instrument and venue. Fundamentally different measures sharing a name; apply the Finance meaning to the Trading clause of a question and the calculation completes without error and returns a number that is precisely wrong. Detection cannot wait for the crossing, and a bootstrap hides here worth naming: the agent notices that a word's meaning shifted only if it already holds both meanings, yet it loads a domain's meanings only after noticing. The estate resolves the circle with a thin, always-resident polysemy index — every glossary term carrying more than one domain sense, one line per sense, glossary-derived and cheap to hold — so the shift is detectable before the full target register loads, and the pivot asks "which volume?" instead of guessing.

The bridge then decides what the composed view means. Bridges are governed mappings: which concepts translate across the boundary, which conflict rules apply where the domains disagree — Finance books a fee as revenue, Trading books the same fee as cost, and the rule says which reading holds in which context — and which temporal alignment makes the join legal at all, a fiscal-calendar table never naively joined to a UTC one. The crossing runs its own pre-flight: access to the target domain stated explicitly rather than silently partial; a thin or missing target layer surfaced as a limitation instead of reasoned over; the polysemous terms mapped before any formula runs; the composed join path declared. And the resolution persists as session state — which domains are active, which terms resolved to which senses, what the composed vocabulary currently is — so "volume," resolved in turn three, still means the same thing in turn ten, and a new domain in turn seven extends the vocabulary rather than resetting it.

The machinery has its proper name: each bridge is a span of theory morphisms through a shared sub-theory, and the composed view the analyst walks is the colimit of the session's diagram — the object Chapter 4 constructed, here doing consumption. Composition takes longer than a single-domain query, and users may read the pause as slowness; it is the price of crossing a boundary with the meaning intact.

=== Whose Security Governs the Joined Cell?

The hard problem is the one clean rooms exist to solve. When a Trading row and a Compliance row are composed across a bridge, *whose* access policy governs the resulting cell? The analyst may be cleared for Trading and not for the Compliance attributes that the bridge would pull in. If composition silently widens her view, the architecture has built a privilege-escalation engine; if it silently drops the restricted columns, she gets a partial answer that looks complete.

The rule is that **composed access is the intersection, declared at the bridge, never inferred at query time.** A cross-domain view exposes a cell only if the consumer is authorized for it in *every* domain that contributed to it; where she is not, the cell is withheld *visibly* — a typed "restricted," not a silent omission — and the view tells her a Compliance grant is what would complete it. In the session above, the Compliance KYC attribute the analyst lacks rights to shows as `restricted`, with the grant that would unlock it named. The bridge is where the row- and column-level policy of the two domains is composed and recorded, the same way the bridge records what maps and what is lost. The system says so out loud rather than guessing.

Two harder problems remain. A labelled hole discloses existence — telling the analyst which grant would complete the view tells her the KYC attribute is there to be seen — so where existence itself is sensitive, the bridge declares the column out of scope entirely rather than advertising the gap. And aggregates over mixed-authorization cells fail closed and say so: a SUM that silently includes what she cannot see is privilege escalation, one that silently excludes is the partial-answer-that-looks-complete this section forbids, so composed aggregates compute over her intersection only and carry that scope on the answer. The genuinely clean-room-shaped questions — thresholds, noise, aggregation floors — belong in a clean room, and the bridge is where that requirement is declared instead of discovered.

None of this is free. Live cross-domain composition is the most expensive query shape in the estate — as-of joins across bitemporal domains, composed at request time — and the five bars "The Death of the Dashboard" will set for the only-interface commitment apply here in full: an explore that takes five minutes against a ten-second bypass loses, governance and all. So the layer owns a latency budget per hop, aggregate awareness routes to rollups wherever they exist, and it leans hard on bridge quality — a missing or stale bridge surfaces as a gap the analyst hits mid-pivot, not a wrong number, which is the right failure but a failure still. Materialize the highest-traffic cross-domain questions as governed flat Gold tables — the answer ledger tells you which questions earn it, and the colimit tells you how their joins are governed — and keep the live explore for the long tail nobody anticipated.

=== Who This Is For

Two consumers, the same surface. The **business user** gets genuine self-serve: pivot anywhere the bridges reach, and where they do not, the gap is named and becomes the next morphism to write. The **data scientist** gets cross-domain *feature discovery* — the ability to find and assemble features that span domains (settlement behavior from Custody against trading patterns from Trading against KYC recency from Compliance) without first standing up a join nobody will govern. Both are doing the same thing: walking the glued ontology. What separates an organization that can do this from one that cannot is whether the bridges exist — the connective tissue from "Multi-Domain Composition," now earning its keep as the thing that makes the next question free.

One piece of machinery has been borrowed on credit throughout: every crossing joined "at the right time grain," as if time were simple. It is not — a bridge that joins a fiscal-calendar domain to a UTC one, or yesterday's belief to today's row, is exactly where meaning breaks silently — and the machinery that keeps it from breaking is the next chapter.
